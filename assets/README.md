# Product screenshots

These are unedited captures of the native macOS application taken on 2026-09-08.
They show a fictional demo repository named Aurora with invented contributor names
and sample code. No customer repository or Branchline application source is shown.

- `history.png`: expandable branches, graph columns, and the commit inspector.
- `diff.png`: a file diff replacing the full graph area, with the file list still visible.

The screenshots demonstrate the workspace UI. They do not show activation or imply
that a distributable build is available.
